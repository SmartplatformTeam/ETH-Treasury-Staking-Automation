---
- name: Deploy Control Plane MVP to one Ubuntu team server
  hosts: team_server
  gather_facts: true
  roles:
    - role: control_plane
      vars:
        control_plane_install_node: true
        control_plane_manage_local_postgres: true
        control_plane_schema_mode: push
        control_plane_seed_database: true
