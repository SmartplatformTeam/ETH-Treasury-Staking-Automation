# 2026-05-19 P2.3-S1 Survey — Approval / AuditLog / Automation 현황

Phase 2-3 (Approval write API) 계획을 잠그기 위한 사전 조사. 코드 변경 없음. 모든 경로는 `strange-keller-a47fa3` 워크트리 기준.

## 1. Approval 모델 (이미 구현됨)

[packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma):

- `enum ApprovalStatus` (L74) — `REQUESTED`, `IN_REVIEW`, `APPROVED`, `REJECTED`, `CANCELLED`
- `enum ApprovalPolicyType` (L82) — `DEPOSIT_REQUEST`, `DKG_CEREMONY`, `SAFE_PROPOSAL`, `SIGNER_BINDING`, `ROLLOUT`, `CHARON_ARTIFACT_STAGE`
- `model Approval` (L339):
  - `resourceType`, `resourceId`, `policyType`, `currentStep` (default 1), `finalStatus` (default REQUESTED)
  - `requestedById`, `approvedById?`, `rejectedById?`
  - `clusterId?`, `hostId?`, `automationOperation?` — automation 매칭에 사용
  - `depositRequestId?` — 자금 흐름 연동
- `model AuditLog` (L453): `actorId?`, `actionType`, `resourceType`, `resourceId`, `diff Json?`

**이미 있는 것**: 결정자 user FK, 정책 타입, automation cluster/host/operation 매칭 슬롯.

**없는 것**: reject 사유 컬럼, 결정 시각 컬럼(separate from updatedAt), 다단계 step 정의 테이블.

## 2. Approval API (read-only 구현됨)

[apps/api/src/modules/workflows/approvals.controller.ts](../../apps/api/src/modules/workflows/approvals.controller.ts):

- `GET /approvals` — status/policyType/limit 필터 + `@RequirePermissions("approvals:read")`
- 컨트롤러 클래스 레벨 guard라 다른 endpoint도 `approvals:read`로 막힘 → 결정 endpoint는 메서드 레벨 `@RequirePermissions("approvals:decide")` 덮어쓰기 필요

[apps/api/src/modules/workflows/workflows.service.ts](../../apps/api/src/modules/workflows/workflows.service.ts):

- `listApprovals()` 만 존재. 결정/생성 메서드 없음.

`apps/api/src/app.module.ts:30` 에서 `ApprovalsController` 등록, `:37` `WorkflowsService` provider.

**없는 것**:
- `POST /approvals` (자료 신규 등록)
- `POST /approvals/:id/decide` 또는 `:id/approve` + `:id/reject`
- `GET /approvals/:id` 상세

## 3. Automation ↔ Approval 검증 (이미 구현됨)

[apps/api/src/modules/automation/automation.service.ts](../../apps/api/src/modules/automation/automation.service.ts):

- `safeInfraOperations` (L28) — verify-baseline, render-runtime, verify-runtime, rollout-dry-run, …
- `expectedApprovalPolicy()` (L44) — `STAGE_ARTIFACTS_EXECUTE` → `CHARON_ARTIFACT_STAGE`, 그 외 execute는 `ROLLOUT`
- `validateApprovalIfRequired()` (L278):
  1. execute op 아니면 통과
  2. `approvalId` 필수
  3. `Approval.finalStatus === APPROVED`
  4. `policyType` 매칭
  5. `clusterId`, `hostId`, `automationOperation` 일치 검증
- `writeAuditLog()` (L331) — `resourceType: "AutomationRun"` 고정

**의미**: API 측 게이트는 이미 잠겨 있음. 누군가 `Approval.finalStatus`를 `APPROVED`로만 바꿔주면 즉시 execute가 동작. Phase 2-3는 그 "바꾸는 방법"을 정의.

## 4. AuditLog write 사용처

- [automation.service.ts](../../apps/api/src/modules/automation/automation.service.ts):202 — `AUTOMATION_RUN_CREATED`
- [automation.service.ts](../../apps/api/src/modules/automation/automation.service.ts):255 — `AUTOMATION_CANCEL_REQUESTED`
- `cdvn-health-sync.service.ts` — health sync 이벤트 (Phase 2-1 잔재)

**없는 것**: approval 관련 write. P2.3에서 `APPROVAL_DECISION_RECORDED` 류 actionType 추가.

## 5. 인증/RBAC

[packages/domain/src/auth.ts](../../packages/domain/src/auth.ts):

- `RbacPermission` (L19) — `approvals:decide` 정의됨
- 권한 매트릭스 (L108): 오직 `ADMIN`, `APPROVER`만 `approvals:decide` 보유
- `TREASURY_OPERATOR`, `INFRA_OPERATOR`는 `approvals:read` 만

[apps/api/src/modules/auth/auth.decorators.ts](../../apps/api/src/modules/auth/auth.decorators.ts):
- `@RequirePermissions(...)` 메타데이터 데코레이터
- `@CurrentSession()` 파라미터 데코레이터로 `AuthSession` 주입

`rbac.guard.ts` 가 `hasPermission()` 으로 가드 처리 (controller 클래스/메서드 모두 적용).

[packages/db/prisma/seed.ts](../../packages/db/prisma/seed.ts):24 — `infra.operator@treasury.example`, `approver.1@treasury.example` 등 6명 stub user 시드됨.

## 6. Web UI (read-only)

[apps/web/src/app/approvals/page.tsx](../../apps/web/src/app/approvals/page.tsx):
- `<DataTable>` 4컬럼 (resource, policy, status, requestedBy)
- 결정 버튼 없음, 상세 페이지 없음

[apps/web/src/lib/workflows.ts](../../apps/web/src/lib/workflows.ts):95 — `loadApprovals()` GET 호출.

## 7. Domain 모델 부족

[packages/domain/src/index.ts:57](../../packages/domain/src/index.ts):

```
export type ApprovalRow = {
  resource: string;
  policy: string;
  status: string;
  requestedBy: string;
};
```

UI 표시용 단순 row 타입. 결정 input/detail 타입은 미정의.

## 시드 데이터

seed.ts:307 가 `DepositRequest`에 묶인 단일 Approval을 만들지만 — `policyType: DEPOSIT_REQUEST`, `finalStatus: IN_REVIEW`. **execute automation을 테스트하려면 `policyType: ROLLOUT` + `automationOperation: ROLLOUT_EXECUTE` + cluster/host 채운 Approval이 필요.** 별도 seed 추가 권장.

## 핵심 차이 요약 (P2.3가 메우는 갭)

| 영역 | 현재 | Phase 2-3 후 |
|---|---|---|
| Approval 생성 | seed에서만 | `POST /approvals` 가능 (execute op 사람 요청 흐름) |
| Approval 결정 | DB 수동 | `POST /approvals/:id/{approve,reject}` |
| AuditLog | automation 관련만 | approval 결정도 기록 |
| Web UI | 목록만 | 상세 + 결정 버튼 (단, P2.3 범위 확정 필요) |
| 권한 | `:decide` 정의만 | 컨트롤러에서 실제 사용 |
| 정책 매칭 | automation에서만 검증 | 결정 endpoint에서도 정합성 검증 (e.g. 자기 자신 approve 금지) |
