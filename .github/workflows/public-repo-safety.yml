name: Public Repo Safety

on:
  push:
  pull_request:

jobs:
  safety-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run public repo safety check
        run: scripts/check-public-repo-safety.sh
